# Dependencies
node_modules/

# Build output
dist/
build/
coverage/

# Environment
.env*
!.env.example

# OS / Editor
.DS_Store
*.log
*.swp
.vscode/
.idea/

# TypeScript cache
*.tsbuildinfo

# Local tooling. The rest of .claude/ is shared and committed: CLAUDE.md's
# skills and agents live there (.claude/skills, .claude/agents).
.claude/worktrees/
.claude/settings.local.json
.firebase/

# Unminified build used for profiling
dist-dev/

# Scratch space for measurement runs (scripts/detail.mjs inputs and outputs).
scratch/

# A second checkout of this repo, nested inside it. Not part of the tree, but
# `git add -A` has swept it into a commit twice; ignoring it is cheaper than
# remembering.
/chromaglass/
gallery/
controls/
